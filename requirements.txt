# Core numerics / plotting -- used by every figures/fig*.py script
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Used by figures/fig4_statistical_analysis_real.py (PCA, t-SNE, KMeans)
scikit-learn>=1.3

# Used by scripts/real_data.py to pull the real ibm_sherbrooke calibration
# snapshot and to transpile real circuits onto its coupling map
qiskit>=1.0
qiskit-ibm-runtime>=0.23

# Used by scripts/real_data.py for the SupermarQ capability-feature radar
# (figures/fig3_circuit_benchmarks_real.py, panel d)
supermarq>=0.1.9
